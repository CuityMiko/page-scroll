var pageScroll = (function (w) {
  var doc = w.document;

  var pageScroll = {
    init: function () {

    },
    getScrollPos: function (win) {
      win = win || w;


    }
  };

  return pageScroll;
}(window));
